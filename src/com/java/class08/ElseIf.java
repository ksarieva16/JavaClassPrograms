package com.java.class08;

public class ElseIf {
    public static void main(String[] args) {

        //hasLicense
        //speedLimit
        //if hasLicence and speedLimit is less than 80
        //then say Safe Journey

        //if hasLicence and speedLimit is more than80
        //then give speeding ticket

        //if doesnt have license speedLimit is less than 80
        //then print out tow the car

        //if no license and speedLimit is more than 80
        //call to court session.

        boolean hasLicense = true;
        int speed = 70;

        if (hasLicense && speed < 80) {

        }

        else if (hasLicense && speed > 80) {

        }

        else {

        }



    }
}
